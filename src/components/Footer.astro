---
import { SITE } from "../site";

interface Props {
  /** Auf der Landingpage bleibt der Blog-Link weg, damit nichts vom Opt-in ablenkt. */
  minimal?: boolean;
  /** Auf Verkaufsseiten kommen AGB und Widerrufsbelehrung dazu (Pflicht). */
  verkauf?: boolean;
}
const { minimal = false, verkauf = false } = Astro.props;
---

<footer class="footer">
  <div class="wrap footer-in">
    <div>
      <span class="brand">Bleib Papa<span class="dot">.</span></span>
      <p class="tag">{SITE.claim}</p>
    </div>
    <nav aria-label="Rechtliches">
      {!minimal && !verkauf && <a href="/blog/">Blog</a>}
      <a href="/impressum/">Impressum</a>
      <a href="/datenschutz/">Datenschutz</a>
      {verkauf && <a href="/agb/">AGB</a>}
      {verkauf && <a href="/widerruf/">Widerruf</a>}
      <a href="/kontakt/">Kontakt</a>
    </nav>
  </div>
</footer>
